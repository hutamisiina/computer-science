We experienced an introduction to programming using Python as an example. Specifically, we created our first object in Python and learned that objects have types, and the operations available depend on the type. We also experienced simple variable assignment using the shell (IPython).
